# PantryPal Mobile App

This directory contains the Flutter application scaffold used by the Nx workspace.

Before running mobile targets:

1. Install Flutter and Dart.
2. Run `flutter pub get` in this directory or `npm run bootstrap` at the repo root.
3. Generate platform folders with `flutter create . --platforms=ios,android` if you need native platform projects before deeper mobile work starts.

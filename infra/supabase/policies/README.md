# Supabase Policies

Keep row-level security policies grouped by bounded context as the schema evolves.

Expected future policy split:

- `households.sql`
- `inventory.sql`
- `shopping.sql`
- `subscriptions.sql`
